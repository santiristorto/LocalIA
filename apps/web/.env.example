# LocalIA Web — variables de entorno
# Copiar como .env y completar. Nunca versionar el .env real.

VITE_API_BASE_URL=http://localhost:4000/api/v1

# REQUERIDO desde el Sprint 1A — Panel de Supabase: Project Settings > API.
# Sin esto la app lanza un error explícito al arrancar (ver shared/lib/env.ts).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
