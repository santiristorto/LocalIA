/**
 * @localia/types
 *
 * Contrato compartido entre `apps/api` y `apps/web`: esquemas Zod y los tipos
 * inferidos a partir de ellos. Deliberadamente vacío en el Sprint 0 — el primer
 * esquema real (por ejemplo, el de `tenants`) se agrega en el Sprint 1, cuando
 * exista la primera entidad de negocio persistida.
 */

export {};
